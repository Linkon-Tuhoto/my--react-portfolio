// tailwind.config.js
/*module.exports = {
  theme: {
    extend: {
      keyframes: {
        typing: {
          "0%": { width: "0%" },
          "100%": { width: "100%" },
        },
        blink: {
          "0%,100%": { borderColor: "transparent" },
          "50%": { borderColor: "#a855f7" },
        },
      },
      animation: {
        typing: "typing 3s steps(20) forwards",
        cursor: "blink 1s step-end infinite",
      },
    },
  },
};

export default {
  content: [
    "./index.html",
    "./src/**//*.{js,ts,jsx,tsx}"
  ],
  theme: {
    extend: {
      keyframes: {
        typing: {
          "0%": { width: "0%" },
          "100%": { width: "100%" },
        },
        blink: {
          "0%,100%": { borderColor: "transparent" },
          "50%": { borderColor: "#a855f7" },
        },
      },
      animation: {
        typing: "typing 3s steps(18) forwards",
        blink: "blink 1s step-end infinite",
      },
    },
  },
  plugins: [],
}*/
/* @type {import('tailwindcss').Config} */
